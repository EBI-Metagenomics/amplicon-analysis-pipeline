Changes in component 'ebi-metagenomics/mapseq'
'modules/ebi-metagenomics/mapseq/meta.yml' is unchanged
Changes in 'mapseq/main.nf':
--- modules/ebi-metagenomics/mapseq/main.nf
+++ modules/ebi-metagenomics/mapseq/main.nf
@@ -1,12 +1,12 @@
 
 process MAPSEQ {
     tag "$meta.id"
-    label 'process_single'
+    label 'medium'
 
     conda "bioconda::mapseq=2.1.1"
     container "${ workflow.containerEngine == 'singularity' && !task.ext.singularity_pull_docker_container ?
-        'https://depot.galaxyproject.org/singularity/mapseq:2.1.1--ha34dc8c_0':
-        'biocontainers/mapseq:2.1.1--ha34dc8c_0' }"
+        'https://depot.galaxyproject.org/singularity/mapseq:2.1.1b--h3ab3c3b_0':
+        'biocontainers/mapseq:2.1.1b--h3ab3c3b_0' }"
 
     input:
     tuple val(meta), path(subunit_reads)
@@ -25,16 +25,17 @@
 
     """
     mapseq \\
+        -seed 12 \\
         $subunit_reads \\
         $db_fasta \\
         $db_tax \\
         -nthreads $task.cpus \\
         $args \\
-        > ${prefix}.mseq
+        > ${prefix}_${task.ext.db_label}.mseq
 
     cat <<-END_VERSIONS > versions.yml
     "${task.process}":
-        mapseq: \$(mapseq -h 2>&1 | head -1 | cut -d" " -f3 | cut -d"v" -f2)
+        mapseq: \$(mapseq -h 2>&1 | head -1 | cut -d" " -f2 | cut -d"v" -f2)
     END_VERSIONS
     """
 
@@ -47,7 +48,7 @@
 
     cat <<-END_VERSIONS > versions.yml
     "${task.process}":
-        mapseq: \$(mapseq -h 2>&1 | head -1 | cut -d" " -f3 | cut -d"v" -f2)
+        mapseq: \$(mapseq -h 2>&1 | head -1 | cut -d" " -f2 | cut -d"v" -f2)
     END_VERSIONS
     """
 }

************************************************************
